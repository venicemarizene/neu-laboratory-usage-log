
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user (Professor or Admin) in the system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Firebase UID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "User email address."
        },
        "role": {
          "type": "string",
          "enum": ["professor", "admin"],
          "description": "User role."
        },
        "status": {
          "type": "string",
          "enum": ["active", "blocked"],
          "description": "Account status."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of account creation."
        }
      },
      "required": ["id", "email", "role", "status", "createdAt"]
    },
    "Log": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Log",
      "type": "object",
      "description": "Records laboratory usage sessions.",
      "properties": {
        "id": {
          "type": "string"
        },
        "professorEmail": {
          "type": "string",
          "format": "email"
        },
        "roomNumber": {
          "type": "string"
        },
        "loginTime": {
          "type": "string",
          "format": "date-time"
        },
        "logoutTime": {
          "type": "string",
          "format": "date-time",
          "nullable": true
        },
        "duration": {
          "type": "number",
          "description": "Duration in minutes."
        },
        "status": {
          "type": "string",
          "enum": ["active", "completed"]
        }
      },
      "required": ["professorEmail", "roomNumber", "loginTime", "status"]
    }
  },
  "auth": {
    "providers": ["google", "password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "Stores user metadata. Admins can read/write all; users can read their own."
        }
      },
      {
        "path": "/logs/{logId}",
        "definition": {
          "entityName": "Log",
          "schema": { "$ref": "#/backend/entities/Log" },
          "description": "Records of laboratory entry. Admins can read all; Professors can create for themselves."
        }
      }
    ]
  }
}
